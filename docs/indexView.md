### 💡 教練課後統整與驗收網址指南

請直接用上面產出的這份程式碼完整覆蓋你的 `src/router/index.ts`。

覆蓋存檔後，你的專案就同時具備了三個清晰的門牌位址（Routes）：

1. **`http://localhost:5173/`** ➔ 進入前台業務櫥窗 (`HomeView.vue`)
2. **`http://localhost:5173/admin`** ➔ 進入後台管理戰情室 (`AdminView.vue`)
3. **`http://localhost:5173/design-system`** ➔ 進入我們剛剛打造的 UI 設計系統 15 宮格展示頁 (`DesignSystemView.vue`)

這樣既不會破壞你既有的後台功能，又能隨時切換到 `/design-system` 檢視 **70/25/5 三爵鋼鐵藍 × 琥珀璀璨金** 的雙主題切換與卡片效果！存檔後開啟瀏覽器輸入 `http://localhost:5173/design-system` 看看，期待你跟我分享成果！
